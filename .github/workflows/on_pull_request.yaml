name: On Pull Request

on:
  pull_request:

jobs:

  get-rocks-modified-and-build-scan-test:
    name: Get ROCKs modified and build-scan-test them
    uses: canonical/charmed-kubeflow-workflows/.github/workflows/get-rocks-modified-and-build-scan-test-publish.yaml@split-build-scan-test-to-smaller-workflows
    permissions:
      pull-requests: read
    secrets: inherit
    with:
      microk8s-channel: 1.26/stable
      juju-channel: 3.1/stable
      python-version: "3.8"
